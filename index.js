const dataBase = require('./db');
// add in the remaining required stuff

// create a function to develope and express a logo and interface to engage in interactivity with the user

// create a function to sift through the stored seeded materiel
// use that function to show who works under who and where they are within any of the departments

// create a function to delete/add an empolyee and the ablity to assign them to any department or manager

// create a function to add/remove departments 

// create a function to update employees roles